# Digest — 2026-04-28

## Tổng quan session

Trong session này, repo đã được chuẩn hóa theo mô hình long-term hơn: có master guide tổng quát, docs entrypoint rõ hơn, config/topics được cập nhật, search/runtime được chuyển sang ưu tiên PostgreSQL trong DB mode, và wiki log được bổ sung để phản ánh các thay đổi vừa rồi.

## Thay đổi chính

- Thêm `AI_CODING_WIKI_GUIDE.md` làm tài liệu tham chiếu tổng quát cho toàn dự án
- Viết lại `CLAUDE.md` và `AGENTS.md` để đồng bộ policy source-of-truth / search / sync / ACL / Obsidian role
- Cập nhật `README.md` để trỏ rõ tới master guide
- Mở rộng `config.yaml` với topic `Knowledge Ops & Productization` và các cờ vận hành mới
- Chuẩn hóa `wiki/LOG.md` với các entry phản ánh workflow mới
- Cập nhật runtime search để DB mode ưu tiên PostgreSQL search, file mode vẫn dùng cho dev/debug

## Tác động lên kiến trúc

### Content model
- Markdown tiếp tục là source of truth cho content
- Obsidian giữ vai trò authoring/exploration
- DB là derived state cho search, permissions, audit, và runtime

### Search
- DB mode hiện là đường chính cho search
- File mode vẫn còn để phục vụ local/dev
- Kiến trúc này tốt hơn cho tốc độ và khả năng mở rộng

### Operability
- Có thêm dấu vết log cho các thay đổi mới
- Guide tổng giúp giảm drift giữa docs và implementation
- Cấu hình topic mới mở đường cho các page về vận hành/search/productization

## Việc nên làm tiếp

1. Bổ sung schema/index cụ thể cho full-text search và trigram trên PostgreSQL
2. Nếu cần, thêm `page_revisions` hoặc lịch sử revision để trace thay đổi tốt hơn
3. Mở rộng `wiki/INDEX.md` với các page về Knowledge Ops / Productization khi có nội dung phù hợp
4. Chuẩn hóa `outputs/queries/` nếu workflow query được dùng thường xuyên

## Ghi chú

- `wiki/LOG.md` vẫn là append-only log của quá trình phát triển knowledge base
- `outputs/digest-2026-04-28.md` ghi lại thay đổi của session này để dễ review sau này
